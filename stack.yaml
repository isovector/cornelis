# GHC 9.2.5
# Update `defaultGHCVersion` in `./flake.nix` when updating to resolver with different GHC version.
resolver: lts-20.06

packages:
- .

extra-deps:
- ../diff-loc
- levenshtein-0.2.1.0
- ../nvim-hs


nix:
  packages: [zlib]
